{
  "name": "banking-api",
  "version": "1.0.0",
  "description": "Sample banking API with tests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "cross-env NODE_ENV=test jest --coverage",
    "test:watch": "cross-env NODE_ENV=test jest --watch",
    "start:inspect": "node --inspect=0.0.0.0:9229 server.js",
    "test:coverage": "nyc jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
